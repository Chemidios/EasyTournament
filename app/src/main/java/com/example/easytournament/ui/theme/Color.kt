package com.example.easytournament.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta MediaMarkt
val MMRed = Color(0xFFDF0000) // El rojo corporativo
val MMWhite = Color(0xFFFFFFFF)
val MMBlack = Color(0xFF000000)
val MMBordeaux = Color(0xFF800000)

// Modo Claro
val LightBackground = Color(0xFFF5F5F5) // Un gris muy muy claro para que el blanco destaque
val LightSurface = Color(0xFFFFFFFF)
val LightOnSurface = Color(0xFF1A1A1A)

// Modo Oscuro
val DarkBackground = Color(0xFF121212) // Negro material
val DarkSurface = Color(0xFF1E1E1E)    // Gris oscuro para tarjetas
val DarkOnSurface = Color(0xFFEEEEEE)   // Texto casi blanco

